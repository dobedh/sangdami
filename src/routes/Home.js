import React from "react";

const Home = ({ userObj }) => {
  return (
    <div>
      <h4> 안녕! </h4>
    </div>
  );
};

export default Home;

/*
      <Link exact path to="/post">
        글쓰기
      </Link> */
