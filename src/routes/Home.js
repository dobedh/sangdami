import React, { useState } from "react";
import Auth from "../components/Auth";
import { authService } from "../fbase";

const Home = () => {
  return (
    <div>
      <h4>Welcome Home! </h4>
    </div>
  );
};

export default Home;
